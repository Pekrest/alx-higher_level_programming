#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    """
    a function that adds 2 tuples.
    """
    x = tuple_x + (0, 0)
    y = tuple_y + (0, 0)
    result = (x[0] + y[0], x[1] + y[1])
    return result
